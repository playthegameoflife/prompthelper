/**
 * Central config for backend URL. Update here and in background.js (service worker
 * can't load this file) when changing the API / payment server.
 */
var BACKEND_API_URL = 'https://api-clyep56cdq-uc.a.run.app';
