{
  "name": "prompt-helper-gemini",
  "version": "1.6.0",
  "description": "Intelligent prompt refinement for AI chats. Enhance Text, Code, Image, and Video prompts with one click.",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist",
    "pack": "npm run build && cd dist && zip -r ../prompt-helper-gemini-chrome-web-store.zip . && echo '\n>>> Upload: prompt-helper-gemini-chrome-web-store.zip to Chrome Web Store (load unpacked from dist/ to test).'"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "terser": "^5.26.0"
  },
  "private": true,
  "dependencies": {
    "firebase": "^12.8.0"
  }
}
